/**
 * Hex values for APIs that require string colors (QR, PDF, some chart libs).
 * Tune these when you change `--dash-*` in `app/globals.css` so print/PDF stays aligned.
 */
export const DASH_INK_HEX = "#1e2a38";
